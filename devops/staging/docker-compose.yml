version: "3.9"

services:
  # 🌐 API principal - DEVELOP
   appointments-api-develop:
    container_name:  appointments-api-develop
    build:
      context: ../..
      dockerfile: Web_back/Dockerfile
    restart: always
    env_file:
      - .env
    ports:
      - "5100:8080"
    networks:
      -  appointments_network
    labels:
      - com. appointments.environment=develop

networks:
   appointments_network:
    external: true