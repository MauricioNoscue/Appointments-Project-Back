pipeline {
    agent any

    environment {
        DOTNET_CLI_HOME = '/var/jenkins_home/.dotnet'
        DOTNET_SKIP_FIRST_TIME_EXPERIENCE = '1'
        DOTNET_NOLOGO = '1'
        PROJECT_PATH = 'Web_back/Web_back.csproj'
    }

    stages {

        // =======================================================
        // 1️⃣ CHECKOUT
        // =======================================================
        stage('Checkout código fuente') {
            steps {
                echo "📥 Clonando repositorio desde GitHub..."
                checkout scm
                sh 'ls -R devops || true'
            }
        }

        // =======================================================
        // 2️⃣ DETECTAR ENTORNO SEGÚN LA RAMA
        // =======================================================
        stage('Detectar entorno') {
            steps {
                script {
                    def branch = env.BRANCH_NAME ?: env.GIT_BRANCH?.replaceFirst(/^origin\//, '') ?: 'develop'

                    switch (branch) {
                        case 'main':    env.ENVIRONMENT = 'prod';     break
                        case 'staging': env.ENVIRONMENT = 'staging';  break
                        case 'qa':      env.ENVIRONMENT = 'qa';       break
                        default:        env.ENVIRONMENT = 'develop';  break
                    }

                    env.ENV_DIR = "devops/${env.ENVIRONMENT}"
                    env.COMPOSE_FILE = "${env.ENV_DIR}/docker-compose.yml"
                    env.ENV_FILE = "${env.ENV_DIR}/.env"
                    env.DB_COMPOSE_FILE = "appointments-db/docker-compose.yml"

                    echo """
                    ✅ Rama detectada: ${branch}
                    🌎 Entorno asignado: ${env.ENVIRONMENT}
                    📄 Compose file (API): ${env.COMPOSE_FILE}
                    📁 Env file (API): ${env.ENV_FILE}
                    🗄️ Compose file (DB): ${env.DB_COMPOSE_FILE}
                    """

                    if (!fileExists(env.COMPOSE_FILE)) {
                        error "❌ No se encontró ${env.COMPOSE_FILE}"
                    }
                }
            }
        }

        // =======================================================
        // 3️⃣ COMPILAR Y PUBLICAR .NET
        // =======================================================
        stage('Compilar .NET dentro de contenedor SDK') {
            steps {
                script {
                    docker.image('mcr.microsoft.com/dotnet/sdk:9.0')
                        .inside('-v /var/run/docker.sock:/var/run/docker.sock -u root:root') {
                            sh """
                                echo '🔧 Restaurando dependencias .NET...'
                                dotnet restore ${PROJECT_PATH}
                                dotnet build ${PROJECT_PATH} --configuration Release
                                dotnet publish ${PROJECT_PATH} -c Release -o ./publish
                            """
                        }
                }
            }
        }

        // =======================================================
        // 4️⃣ CONSTRUIR IMAGEN DOCKER
        // =======================================================
        stage('Construir imagen Docker') {
            steps {
                sh """
                    echo '🐳 Construyendo imagen Docker para Appointments-Project-Back (${env.ENVIRONMENT})'
                    docker build -t appointments-api-${env.ENVIRONMENT}:latest -f Web_back/Dockerfile .
                """
            }
        }

        // =======================================================
        // 5️⃣ PREPARAR RED Y BASE DE DATOS LOCAL
        // =======================================================
        stage('Preparar red y base de datos') {
            steps {
                script {
                    sh """
                        echo '🌐 Creando red externa compartida (si no existe)...'
                        docker network create appointments_network || true

                        echo '🗄️ Levantando stack de bases de datos local...'
                        docker compose -f ${env.DB_COMPOSE_FILE} up -d

                        echo '⏳ Esperando que SQL Server inicie correctamente...'
                        sleep 25
                    """
                }
            }
        }

        // =======================================================
        // 6️⃣ DESPLEGAR API LOCAL
        // =======================================================
        stage('Desplegar citas API LOCAL') {
            steps {
                sh """
                    echo '🚀 Desplegando entorno local: ${env.ENVIRONMENT}'
                    docker compose -f ${env.COMPOSE_FILE} --env-file ${env.ENV_FILE} up -d --build
                """
            }
        }

        // =======================================================
        // 7️⃣ DEPLOY EN AWS SOLO PARA DEVELOP
        // =======================================================
            // stage('Desplegar API en AWS') {
            //     when {
            //         expression { env.ENVIRONMENT == 'develop' }
            //     }
            //     steps {
            //         withCredentials([sshUserPrivateKey(credentialsId: 'aws_ssh_key', keyFileVariable: 'SSH_KEY')]) {
            //             sh """
            //                 echo '🚀 Iniciando despliegue remoto en AWS...'

            //                 ssh -o StrictHostKeyChecking=no -i "$SSH_KEY" ubuntu@3.92.92.181 "
            //                     set -e

            //                     echo '📁 Navegando al proyecto...'
            //                     cd /home/ubuntu/appointments-back || exit 1

            //                     echo '📦 Actualizando código...'
            //                     git reset --hard
            //                     git pull

            //                     echo '🐳 Reconstruyendo y levantando contenedores (Compose v2)...'
            //                     docker compose -f devops/develop/docker-compose.yml --env-file devops/develop/.env up -d --build --force-recreate --remove-orphans

            //                     echo '✔️ Deploy completado en AWS!'
            //                 "
            //             """
            //         }
            //     }
            // }
    }

    post {
        success {
            echo "🎉 Despliegue completado correctamente para ${env.ENVIRONMENT}"
        }
        failure {
            echo "💥 Error durante el despliegue en ${env.ENVIRONMENT}"
        }
        always {
            echo "🧹 Limpieza final del pipeline completada."
        }
    }
}
